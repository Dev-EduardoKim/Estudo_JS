/**
 * Crie uma variável velocidade = 100 (km/h) e tempo = 2 (horas). Calcule a distância percorrida (velocidade x tempo).
 */

let KmPorHora = 100;
let tempoHora = 2;

const DistanciaPercorrida = KmPorHora * tempoHora;

console.log(`A distancia percorrida foi de ${DistanciaPercorrida} Km`);