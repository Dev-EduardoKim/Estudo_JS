/**
 * Crie as variáveis precoIngresso = 30 e quantidadePessoas = 4. Mostre no console o total a pagar.
 */

let precoIngresso = 30;
const QUANTIDADE_PESSOAS = 4;

console.log(`O total a ser pago é R$${precoIngresso * QUANTIDADE_PESSOAS}`);
