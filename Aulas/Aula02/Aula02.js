console.log('Interagir com o HTML.');

// A interação com o html pode ser usado a partir da tag <script>. Usando o atributo src para passar o caminho do arquivo js.
// Também com a tag <script> , entre a tag de abertura e fechamento pode ser escrito código js.
// A tag <script> pode estar dentro do <head> e/ou <body>. Mas para não haver erros , colocar no <body>.

alert('Função que cria um balão flutuante na página.'); // O node não consegue debbugar a função alert()

